apiVersion: v1
kind: Service
metadata:
  name: secure-observable-api
  labels:
    app.kubernetes.io/name: secure-observable-api
    app.kubernetes.io/component: api
spec:
  selector:
    app.kubernetes.io/name: secure-observable-api
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
