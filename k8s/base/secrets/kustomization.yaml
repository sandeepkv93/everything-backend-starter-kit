apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Secrets are intentionally not committed as plaintext manifests.
# Create `app-secrets` from `app-secrets.env.template`:
# kubectl -n secure-observable create secret generic app-secrets \
#   --from-env-file=.secrets.k8s.app.env \
#   --dry-run=client -o yaml | kubectl apply -f -
