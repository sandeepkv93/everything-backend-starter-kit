apiVersion: v1
kind: Service
metadata:
  name: everything-backend-api-preview
  namespace: everything-backend
  labels:
    app.kubernetes.io/name: everything-backend-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: rollout
spec:
  selector:
    app.kubernetes.io/name: everything-backend-api
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
